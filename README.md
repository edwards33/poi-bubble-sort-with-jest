## Data Structures in JS using POI and Jest (Bubble Sort Example)

  Install [POI](https://poi.js.org/):

```bash
$ npm i -g poi
```

  Install Jest:

```bash
$ npm i -D jest
```

  Run App and open it in a Web browser:

```bash
$ poi --serve
```

  Test Bubble Sort:

```bash
$ npm test
```
